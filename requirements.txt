# Core Framework
slack-bolt==1.18.0
python-dotenv==1.0.0

# AI and Language Processing
openai>=1.6.1

# Google Services
google-auth==2.23.4
google-auth-oauthlib==1.1.0
google-auth-httplib2==0.2.0
google-api-python-client==2.108.0

# Document Processing - Core (GPT-4o Direct PDF Processing)
# PyPDF2==3.0.1          # REMOVED: Traditional extraction methods eliminated
python-docx==1.1.0
openpyxl==3.1.2

# Document Processing - Enhanced (REMOVED)
# pdfplumber==0.10.3     # REMOVED: GPT-4o Direct provides superior extraction

# OCR Support (REMOVED - GPT-4o handles image-based PDFs)
# pytesseract==0.3.10    # REMOVED: GPT-4o Direct reads images natively
# pdf2image==1.17.0      # REMOVED: No longer needed for OCR pipeline
# Pillow>=10.0.0         # REMOVED: GPT-4o processes images directly

# Vision Integration (GPT Vision + PDF processing)
PyMuPDF==1.23.14

# Web Search
tavily-python==0.7.11

# Utilities
requests==2.31.0

# Production server (optional - Railway can run directly with python app.py)
gunicorn==21.2.0

# For better JSON handling
ujson==5.8.0

flask==2.3.3
pandas>=2.2.0
