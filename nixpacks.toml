[phases.setup]
nixPkgs = ['python39', 'poppler_utils', 'tesseract', 'gcc']

[phases.install]
cmds = ['python -m venv --copies /opt/venv', '. /opt/venv/bin/activate && pip install -r requirements.txt']

[phases.start]
cmd = 'python app.py'